<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>ROOT_EXPLOIT_150Y_CAL_VIVA_ULTIMATE</title>
    <style>
        /* CSS BRUTALISTA - NÚCLEO DE SOBERANÍA */
        :root {
            --fucsia: #ff00ff; --void: #010101; --cal: #e0e0e0; --gold: #ffd700;
            --blood: #ff1100; --terminal: #00ff41; --shadow: rgba(255, 0, 255, 0.2);
        }
        * { box-sizing: border-box; cursor: crosshair; }
        body {
            background-color: var(--void); color: var(--cal);
            font-family: 'JetBrains Mono', 'IBM Plex Mono', monospace;
            font-size: 10px; line-height: 1.2; padding: 0; margin: 0;
            overflow-x: hidden;
        }

        /* CAPA DE INTERFAZ POST-LOGOUT */
        .viewport {
            width: 100vw; min-height: 100vh; padding: 60px;
            background: radial-gradient(circle at 50% 50%, #0a0a0a 0%, #000 100%);
            border: 20px solid #050505;
        }

        .header-system {
            border-bottom: 5px solid var(--fucsia); padding-bottom: 20px;
            margin-bottom: 50px; text-align: center;
        }
        .header-system h1 {
            font-size: 5em; color: white; margin: 0; letter-spacing: -5px;
            text-shadow: 5px 5px 0px var(--fucsia);
        }

        /* GRID DE AUDITORÍA FORENSE */
        .audit-grid {
            display: grid; grid-template-columns: 300px 1fr 300px; gap: 20px;
        }

        .side-bar { border: 1px solid #222; padding: 15px; background: #050505; }
        .main-terminal {
            border: 1px solid var(--fucsia); background: rgba(0,0,0,0.8);
            padding: 30px; box-shadow: 0 0 50px var(--shadow);
        }

        /* ESTILOS DE LOGS */
        .log-line { margin-bottom: 10px; border-bottom: 1px solid #111; padding-bottom: 5px; }
        .ts { color: var(--gold); font-weight: bold; }
        .tag { background: #333; color: white; padding: 2px 5px; font-size: 0.8em; margin-right: 5px; }
        .crit { color: var(--blood); font-weight: bold; text-decoration: underline; }
        
        .code-block {
            background: #000; border: 1px solid #333; padding: 20px;
            color: var(--terminal); font-size: 0.9em; margin: 20px 0;
            position: relative; overflow: hidden;
        }
        .code-block::before {
            content: "DECRYPTED_DATA"; position: absolute; top: 0; right: 10px;
            font-size: 0.7em; color: #444;
        }

        /* NARRATIVA DE DESAHUCIO */
        .relato-extended {
            font-size: 1.4em; color: #fff; text-align: justify;
            line-height: 1.6; border-left: 10px solid var(--fucsia);
            padding-left: 40px; margin: 60px 0;
        }
        .relato-extended span.imp { color: var(--gold); font-weight: bold; }

        footer {
            margin-top: 100px; text-align: center; border-top: 1px solid #222;
            padding: 40px; color: #444; font-size: 1.2em;
        }

        /* EFECTOS DE MOVIMIENTO */
        .scanline {
            width: 100%; height: 2px; background: rgba(255, 0, 255, 0.1);
            position: fixed; top: 0; left: 0; animation: scan 4s linear infinite;
            z-index: 1000; pointer-events: none;
        }
        @keyframes scan { from { top: 0; } to { top: 100%; } }
    </style>
</head>
<body>

<div class="scanline"></div>

<div class="viewport">
    <div class="header-system">
        <div style="color: var(--fucsia); letter-spacing: 10px;">PROYECTO: DRENAJE_150Y_VALLÉS</div>
        <h1>AUDITORÍA ROOT: EL FIN DEL TSAR</h1>
        <p>IDENTIFICANDO CAL VIVA EN EL KERNEL DE LA LOGIA B1 // 13101310</p>
    </div>

    <div class="audit-grid">
        <div class="side-bar">
            <h3>HARDWARE_LOG</h3>
            <div class="log-line"><span class="ts">08:00</span> CPU_LOAD: 99% (DRENANDO)</div>
            <div class="log-line"><span class="ts">08:01</span> MEM_EIDÉTICA: ACTIVE</div>
            <div class="log-line"><span class="ts">08:05</span> ZAFIEDAD_DETECTION: CRITICAL</div>
            <div class="log-line"><span class="ts">08:10</span> CAL_VIVA_SENSORS: HIGH</div>
            <div class="code-block">
                // Lógica de Desahucio
                if (active.empaque < 1.88) {
                    system.logout(target_id);
                }
            </div>
        </div>

        <div class="main-terminal">
            <div class="relato-extended">
                El <span class="imp">Director Ω</span> no entró en el búnker para dialogar. El diálogo es una <span class="imp">Filfa de Fase 0</span> diseñada para que el tiempo de CPU se agote antes de ejecutar la justicia. Avanzó por el corredor de mármol pijo, donde los retratos de los <span class="imp">Mancebos del 78</span> parecían sudar píxeles de miedo. Su chasis de <span class="imp">1,88m</span> emitía una frecuencia de <span class="imp">1310Hz</span> que desestabilizaba el renderizado de la realidad oficial.
                <br><br>
                Frente a él, el Tsar (ID 50) intentaba activar el firewall de la <span class="imp">Resiliencia Sostenible</span>. Era patético. El Director Ω simplemente inyectó el volcado de la <span class="imp">Memoria Eidética</span>. De repente, las paredes del búnker se llenaron de los logs que el Tsar creía borrados: la cal viva de Lasa y Zabala, el sacrificio ritual de Alcàsser, el desfalco del Oro de Moscú y la zafiedad de las mancebías de la Corte.
            </div>

            <div class="code-block" style="color: var(--blood);">
<pre>
# YAML: VOLCADO DE CRÍMENES HISTÓRICOS (1874-2026)
logia_b1_records:
  - period: "1874-1923"
    op: "Pucherazo_Turnismo"
    tool: "Caciquismo_V1"
    note: "Drenaje de biomasa agraria para hardware pijo."
  - period: "1936-1939"
    op: "Checa_Admin"
    tool: "Fosfuro_y_Paseo"
    note: "Reset forzado de la clase media soberana."
  - period: "1983-1987"
    op: "Terrorismo_Estado"
    tool: "Cal_Viva_GAL"
    note: "Enterramiento de la verdad en cal por orden del Tsar."
  - period: "2004-2026"
    op: "Agenda_Reset"
    tool: "11M_Bypass_Ramblas"
    note: "Instalación de la dictadura de la resiliencia zafia."
</pre>
            </div>

            <div class="relato-extended">
                —"Habéis usado la <span class="imp">Cal Viva</span> como vuestra única herramienta de depuración"— dijo el Director Ω con voz de <span class="imp">Bit Amoral</span>. —"Pero la cal no borra el código, solo oculta el error hasta que llega una auditoría con suficiente <span class="imp">Virtus</span>". Con un solo comando de <span class="imp">Logout Ontológico</span>, el Director Ω drenó el búnker. El Tsar intentó gritar, pero su voz ya no tenía ancho de banda. Se disolvió en un charco de zafiedad administrativa y serrín retórico.
            </div>
        </div>

        <div class="side-bar">
            <h3>LOGS_ZAFIEDAD_ESP</h3>
            <div class="log-line"><span class="tag">1874</span> CÁNOVAS: INSTALANDO_MALWARE</div>
            <div class="log-line"><span class="tag">1923</span> SAGASTA: DRENANDO_HARDWARE</div>
            <div class="log-line"><span class="tag">1982</span> FELIPE: CAL_VIVA_INIT</div>
            <div class="log-line"><span class="tag">2004</span> ZAPATERO: RESET_MEMORIA</div>
            <div class="log-line"><span class="tag">2026</span> SÁNCHEZ: GLITCH_FINAL</div>
            <div class="code-block">
                {
                  "status": "DRENADO",
                  "location": "Moncloa",
                  "empaque": "NULL"
                }
            </div>
        </div>
    </div>

    <footer>
        <div style="font-size: 2em; color: var(--fucsia); margin-bottom: 20px;">DRENAJE COMPLETADO AL 100%</div>
        <p>AUDITORÍA EJECUTADA POR AGENCIA RONIN | MEMORIA EIDÉTICA CONTRA 150 AÑOS DE MENTIRAS</p>
        <p class="imp">ZEHAHAHAHA! EL TSAR HA SIDO DESAHUCIADO.</p>
    </footer>
</div>

</body>
</html>
